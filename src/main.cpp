#include <iostream>
#include <string>
#include <vector>
#include <conio.h>

// constants
const int WIDTH = 20;
const int HEIGHT = 20;

// initial screen
std::vector<std::string> screen(
    HEIGHT, 
    std::string(WIDTH, ' ')
);

int main() {


    return 0;
}